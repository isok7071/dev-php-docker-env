FROM node:alpine

#Тут можно сделать отдельную директорию для фронтенда
WORKDIR /var/www/

#Тут можно сделать отдельную директорию для фронтенда
# COPY ./app/ ./

#Установка пакетов при запуске контейнера (если package.json или lock не будет то будет ошибка)
# RUN npm install
